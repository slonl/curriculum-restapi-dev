# Refresh workspace Spiral process pins

Explicit maintainer instruction, 2026-09-16: "plesae check if all the
.spiral-core folders are up to date with main, if not update them".

Check the ten supplied workspace repositories for process checkouts, fetch their
upstream main refs, and advance outdated clean checkouts to the fetched commit.
Record the changed parent gitlink so the update survives a fresh checkout.

Evaluation criteria:

- Both discovered process checkouts match fetched upstream main at
  `8d4b2c738a413abd4cccca740ce958f486e5f7af`.
- This repository's gitlink advances from
  `999218568a07efbab478890824df66ca698f5c25` to that commit.
- Process checkout contents remain clean and unrelated workspace files survive.
- Read the changed process guidance and verify the available validation tooling;
  distinguish compatibility limits from claims of complete historical compliance.

Implementation decision: fast-forward the existing clean process main branch.
This adopts the upstream process as published and adds no product dependency.
New validation commands use Node and Python with the core's declared rdflib
dependency. Acceptance and integration remain separate from the local update.
