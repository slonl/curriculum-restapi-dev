# Update Spiral core pins

Branch: `spiral/CYC-20260916-1ZG1H-1-update-spiral-core`
Status: Active.

## Analyze and plan

The maintainer requested on 2026-09-16: "plesae check if all the
.spiral-core folders are up to date with main, if not update them".
This is an explicit, bounded process-maintenance instruction. No product
behavior or engineering culture selection is involved. No governing multi-cycle
roadmap is confirmed; this maintenance task does not reopen the accepted FO
investigation.

Inspection across the ten workspace repositories found two process checkouts.
After fetching upstream main, SimplyStore already matches
`8d4b2c738a413abd4cccca740ce958f486e5f7af`. This repository pins
`999218568a07efbab478890824df66ca698f5c25`, 43 commits behind with no divergence.
Both process working trees are clean.

Goal: update the older checkout and its parent gitlink to the fetched main tip,
then verify both workspace process checkouts match it. Preserve unrelated work.
The request and evaluation criteria are recorded in REQ-20260916-1ZG1H-2.

The branch starts at `ec8770d`, preserving the accepted CYC-003 investigation
and its September clarification. The authoritative integration branch is main;
this maintenance branch is not itself an integration or acceptance decision.
The existing untracked root AGENTS.md belongs to the prior workspace state.

Use the already-current sibling checkout's allocator to create distributed IDs
before adopting the new pin. Fast-forward the clean process checkout without
rewriting history. Record compatibility findings and verification evidence.

## Act and evaluate

The process checkout was fast-forwarded to the fetched main tip and the parent
gitlink updated. Added the pinned ontology's relation hierarchy to enable Git
snapshot validation, plus integration context for the new manual validation
boundary. EVD-20260916-1ZG1H-3 records the checks and their limits.

Both process checkouts now match upstream main and are clean. The updated core's
14 allocator/integration tests pass; project snapshot validation passes. The
local update goal is ready for evaluation. Human acceptance has not been
recorded, so the cycle remains Active and has not been integrated into main.
