# Workspace Spiral core refresh evidence

Observed on 2026-09-16.

The ten supplied workspace repositories contain two `.spiral-core` checkouts:

| Repository | Before | After |
| --- | --- | --- |
| curriculum-restapi-dev | `999218568a07efbab478890824df66ca698f5c25` | `8d4b2c738a413abd4cccca740ce958f486e5f7af` |
| simplystore | `8d4b2c738a413abd4cccca740ce958f486e5f7af` | unchanged |

Fetched origin/main in both process repositories. The older checkout was 43
commits behind without divergence and was advanced with `git merge --ff-only
origin/main`. Both process working trees are clean and match the fetched main
commit. The other eight workspace repositories have no process checkout.

The parent gitlink records the new pin. The prior CYC-003 branch and untracked
root AGENTS.md are preserved. No application submodule pin changed.

The adopted guidance adds distributed artifact allocation, explicit commitment
before execution, risk selection by upstream impact, and prospective integration
validation with cycle and convergence guards. Existing project IDs remain valid.
No new engineering culture or warning profile was adopted.

Verification:

- `PATH=/tmp/simplystore-spiral-venv/bin:$PATH node --test
  test/spiral-allocator.test.mjs test/spiral-integration.test.mjs` in the updated
  core: **14 passed**. Initial sandbox execution could not spawn the fixtures;
  the permitted rerun completed successfully.
- The new core's `spiral-rdf.py --repo . --tree <staged-tree>` validates the
  staged project graph successfully. Initial baseline validation reported
  `causal-vocabulary-unavailable` because Git tree traversal does not descend
  into the core submodule. `.spiral/vocabulary.ttl` supplies the relation
  hierarchy; its triples are checked against the pinned core ontology.
- New Turtle is parsed with rdflib 7.6.0, and new historical references are
  checked for full hashes, reachable upstream commits and target existence
  before committing. No SHACL shapes are shipped in the adopted core.

These are process-update checks, not a complete provenance or product audit.
Older research artifacts contain unversioned causal links; their historical
compliance is not established by snapshot validation. Hosted enforcement is
not configured in this repository; project context records the manual pre-merge
command and unknown hosting settings. Integration validation remains a final
gate after human cycle acceptance and refresh of the actual main target.
